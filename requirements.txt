# --- Core ---
anthropic>=0.40.0
chromadb>=0.5.0
python-dotenv>=1.0.0

# --- Generation / embeddings via OpenAI (set FDI_LLM_PROVIDER=openai) ---
openai>=1.40.0

# --- Embeddings (local default; offline, no API key) ---
sentence-transformers>=3.0.0

# --- Document parsing ---
pdfplumber>=0.11.0

# --- Chunking / retrieval helpers ---
tiktoken>=0.7.0
rank-bm25>=0.2.2

# --- Web UI ---
streamlit>=1.36.0

# --- Optional: Voyage AI embeddings (finance-tuned). Uncomment to use. ---
# voyageai>=0.3.0
